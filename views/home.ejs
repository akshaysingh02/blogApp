<%-include('partials/header')-%>

<h1>Home</h1>
<p><%= HomeContent %></p>

<ul>
    
   <% posts.forEach((item)=>{  %>
    <h1><%=  item.title %> </h1>
    <p>
        <%= item.content.substring(1, 100) + "..."  %>
         <a href="/posts/<%= item._id %>">Read more</a> 
    </p>
    <%  }) %>
</ul>

<%-include('partials/footer')-%>